# Problem statement Create a matrix having diagonal elements as 1 and all other elements as 0 of size (5, 5). Print the Numpy array.
# Output Format : numpyArray

import numpy as np

a = np.identity(5,dtype=int)
print(a)


